{
  "name": "student-management-mern",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["server", "client"],
  "scripts": {
    "dev": "concurrently \"npm --workspace server run dev\" \"npm --workspace client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --workspace server start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
